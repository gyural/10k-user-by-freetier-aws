spring:
  config:
    activate:
      on-profile: integration
  datasource:
    driver-class-name: org.testcontainers.jdbc.ContainerDatabaseDriver
  sql:
    init:
      mode: never
api:
  key: valid-api-key